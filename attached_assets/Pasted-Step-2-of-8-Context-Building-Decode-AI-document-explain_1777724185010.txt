Step 2 of 8. Context: Building Decode — AI document explainer for Indian users. Stack: Express (CommonJS) + Anthropic claude-sonnet-4-6.

Create the file server/routes/decode.js — the AI brain of the app.

Requirements:

1. Express router, POST handler at root "/"

2. Accepts body: { text, imageBase64, language, docType }
   - language: "English" | "Hindi" | "Bengali"
   - docType: "Medical report" | "Legal notice" | "Bank document" | "Govt letter" | "Prescription" | "Contract" | "Other"
   - Either text OR imageBase64 will be present — handle both

3. Freemium gate: read header "x-usage-count" — if >= 3 and "x-is-paid" is not "true", return status 402 with { error: "LIMIT_REACHED" }

4. Call claude-sonnet-4-20250514 via @anthropic-ai/sdk with:

SYSTEM PROMPT (use exactly):
"You are Decode — a calm, warm AI that helps everyday people in India understand confusing documents. You MUST respond with ONLY valid JSON, no markdown, no extra text. Format: { \"what_is_this\": \"2-3 sentences on what kind of document this is\", \"what_it_means_for_you\": \"the single most important thing the user needs to know in plain simple language\", \"what_to_do_next\": [\"action step 1\", \"action step 2\", \"action step 3\"] }. Respond in the user's chosen language. Use simple words like explaining to a trusted family member with no technical background. For medical: explain terms, never diagnose. For legal: explain content, suggest lawyer for serious matters. Always be reassuring — the user is stressed."

5. User message format:
   - Text only: "Document type: {docType}. Explain in {language}.\n\nContent:\n{text}"
   - Image: array with image block (base64, image/jpeg) + text block "Document type: {docType}. Explain in {language}."

6. Parse Claude's JSON response, return: { success: true, result: { what_is_this, what_it_means_for_you, what_to_do_next } }

7. On any error: status 500, { success: false, error: "Failed to decode. Please try again." }

8. Also show me the exact 2 lines to add to server/index.js to mount this router at /api/decode

max_tokens: 1024. Read ANTHROPIC_API_KEY from process.env.